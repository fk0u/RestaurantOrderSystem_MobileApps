import 'package:flutter/material.dart';

class AppColors {
  // Primary (Green/Matcha)
  static const Color primary = Color(0xFF7CB342); // Matcha Green
  static const Color primaryVariant = Color(0xFF558B2F);
  
  // Secondary
  static const Color accent = Color(0xFFFFB300); // Amber for actions

  // Backgrounds
  static const Color background = Color(0xFFF5F6F8); // Soft Grey
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceLight = Color(0xFFFAFAFA);
  
  // Text
  static const Color textPrimary = Color(0xFF2D2D2D);
  static const Color textSecondary = Color(0xFF757575);
  
  // States
  static const Color success = Color(0xFF43A047);
  static const Color error = Color(0xFFE53935);
  static const Color warning = Color(0xFFFFA000); // Amber 700
  
  // Misc
  static const Color divider = Color(0xFFEEEEEE);
  static const Color overlay = Color(0xAA000000);
}
